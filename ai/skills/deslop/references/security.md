# Security

Unsafe HTML insertion, command or SQL interpolation, leaked secrets, insecure randomness, and unsafe path handling in the changed code.

Reference secret type and location only in findings — never copy secret values.
