# Pi Pretty

Minimal Pi extension to prettify built-in tool rendering:

- `read`: syntax-highlighted content with line numbers.
- `bash`: open-right frame with exit summary on the bottom.
- `ls`: Nerd Font icons and tree-like rows.
- `find` / `grep`: grouped rendering over Pi’s own tool implementations.
- `write` / `edit`: GitHub-style diffs.
