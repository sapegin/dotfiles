# crlf

> Convert Windows line endings to Unix

- Dry run (print files to convert, don’t actually convert them):

`crlf`

- Convert files:

`crlf --force`

- Convert one files:

`crlf {{filepath}} --force`
